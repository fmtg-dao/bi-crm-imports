from config import load_mysql_config
from mysql_client import MySQLClient
from transform_data import map_picklist
from salesforce_client import SalesforceClientCC, load_salesforce_cc_config_from_env
from datetime import datetime, timezone, time, date
from typing import Any, Dict, Optional, Union
from tqdm import tqdm
import json
from pathlib import Path
import urllib.parse
import random

# Error Logging
import logging
from pathlib import Path

# create logs folder
log_path = Path("logs/import_contacts.log")
log_path.parent.mkdir(parents=True, exist_ok=True)

logging.basicConfig(
    filename=log_path,
    level=logging.ERROR,
    format="%(asctime)s | %(levelname)s | %(message)s"
)



def dev_email(email: str | None) -> str | None:
    if not email:
        return None
    return f"{email}.inactive"


def sf_datetime(value: Optional[Union[datetime, date]]) -> Optional[str]:
    if value is None:
        return None

    # If it's a date, convert to datetime at midnight UTC
    if isinstance(value, date) and not isinstance(value, datetime):
        value = datetime.combine(value, time.min, tzinfo=timezone.utc)

    # If it's a datetime without tz, assume UTC
    if value.tzinfo is None:
        value = value.replace(tzinfo=timezone.utc)

    return value.astimezone(timezone.utc).isoformat().replace("+00:00", "Z")


def account_person_row_to_payload() -> None:

    #
    # SELECT cluster_id, _entity_id, first_name, last_name, email, birth_date, 
    #  salutation, gender, middle_name, address, city, postal_code, country, 
    #  phone, birth_place, nationality, `language`, member_id, member_tier, 
    #  source_system, has_active_loyalty, preferred_language
    # FROM int_crm_person_accounts;
    #


    cfg_mysql = load_mysql_config()
    db = MySQLClient(cfg_mysql)

    # SELECT multiple rows
    accounts = db.fetch_all(
        """select * from int_crm_person_accounts"""
    )
    
    #"""select * from int_crm_person_accounts where cluster_id > 1000 order by cluster_id asc"""

    #cnt = len(accounts)
    #print(cnt)

    ls_payload = []


    for row in accounts:

        payload = { 
            'cluster_id': row.get('cluster_id'), 
                'payload':  {
                    # --- Source info ---
                    "SourceSystem__c": row.get('source_system'),
                    "ClusterID__pc": row.get('cluster_id'),

                    # --- Contact (Person Account core fields) ---
                    "FirstName": row.get("first_name"),
                    "LastName": row.get("last_name"),
                    "Salutation": row.get("salutation"),
                    "MiddleName": row.get("middle_name"),
                    #"Suffix": None,

                    "PersonEmail": dev_email(row.get("email")),
                    "PersonMobilePhone": row.get("phone"),
                    #"PersonHomePhone": None,
                    #"PersonOtherPhone": None,
                    #"PersonAssistantPhone": None,

                    #"PersonTitle": row.get("pg_title"),
                    #"PersonDepartment": None,
                    #"PersonAssistantName": None,

                    # --- Person demographics ---
                    "PersonBirthdate": sf_datetime(row.get("birth_date")),
                    "PersonGenderIdentity": row.get("gender"),
                    #"PersonPronouns": None,


                    # --- Billing Address (Account-level, still valid for Person Accounts) ---
                    "BillingStreet": row.get("address"),
                    "BillingCity": row.get("city"),
                    #"BillingState": None,
                    "BillingPostalCode": row.get("postal_code"),
                    #"BillingCountry": None,
                    "BillingCountryCode__c": row.get("country"),

                    # --- Contact fallback ---
                    "Phone": row.get("phone"),

                    # --- Custom Person fields (__pc) ---
                    "BirthPlace__pc": row.get("birth_place"),
                    "NationalityCountryCode__pc": row.get("nationality"),
                    "PreferredLanguage__pc": row.get("preferred_language"),
                    #"RegionCode__pc": None,
                    "SourceSystem__pc": row.get('source_system'),

                    # --- Custom Account / Business metadata ---
                    #"BillingEmail__c":  dev_email(row.get("pg_email")),
                    #"CompanyDebitorID__c": None,
                    #"CompanyID__c": None,
                    #"CompanyRegisterNumber__c": None,
                    #"CompanyTaxID__c": None,
                    #"IATACode__c": None,
                    #"GDSID__c": None,
                    #"iHotelierID__c": None,

                    # --- Flags / integration ---
                    #"IsMasterAccount__c": False,
                    

                }
            }
        
        # remove empty fields
        payload["payload"] = {
            k: v
            for k, v in payload["payload"].items()
            if v is not None
        }

        #print(payload)

        ls_payload.append(payload)

    return ls_payload



if __name__ == "__main__":

    print(f"import_contacts | start time: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")

    contacts = account_person_row_to_payload() #[:1]

    print(f"import_contacts | number of records: {len(contacts)}")


    err_cluster_id = []


    cfg_sfcc = load_salesforce_cc_config_from_env()
    with SalesforceClientCC(cfg_sfcc) as sf:
        sf.authenticate()

        for c in tqdm(contacts, total=len(contacts)):
            
            # upsert strategy - needs cluster_id
            #contact_id = sf.upsert_object_by_external_id('Account', 'ClusterID__pc', c['cluster_id'], c['payload']) 
            #print(contact_id)

            # create account - will created dubs 
            #account_id = sf.create_account_person(c['payload'])
            #print(account_id)
            try:
                # create account - will created dubs 
                account_id = sf.create_account_person(c['payload'])

            except Exception as e:
                logging.exception(f"cluster_id: {c['cluster_id']} | payload: {c['payload']}")
                err_cluster_id.append(c['cluster_id'])

            finally:
                with open("local_data/err_cluster_id.json", "w") as f:
                    json.dump(err_cluster_id, f)
        